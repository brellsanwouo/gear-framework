# Copy this file to .env, then replace the placeholder values.

# Required to execute generated CrewAI or Google ADK workflows.
# Uncomment this line and replace the placeholder with your OpenAI API key.
# OPENAI_API_KEY=sk-your-openai-api-key

# Optional aliases accepted by some frameworks/tools.
OPENAI_KEY=
OPENAI_TOKEN=

# Optional Google/Gemini keys, only needed for workflows that use them.
GEMINI_API_KEY=
GOOGLE_API_KEY=

# The Studio provider is always OpenAI. Leave this empty to let users choose
# among the supported OpenAI mini models, or set one model to lock the choice.
GEAR_STUDIO_MODEL=

# Local Studio execution. Enable only for projects you trust.
GEAR_ENABLE_LOCAL_RUNNER=false
GEAR_RUNNER_TIMEOUT=180
GEAR_RUNNER_CPU_SECONDS=60
GEAR_RUNNER_MEMORY_MB=2048
GEAR_RUNNER_OUTPUT_MB=10

# Stable secret used to sign anonymous participant cookies. Generate with:
# openssl rand -hex 32
GEAR_SESSION_SECRET=replace-with-a-long-random-value
# Enable only when the Studio is served through HTTPS.
GEAR_SESSION_COOKIE_SECURE=false

# PostgreSQL database used by the existing research experiment.
DB_PASSWORD=
DB_HOST=127.0.0.1
DB_PORT=5432
DB_USER=gear_user
DB_NAME=gear_app
DATABASE_URL=postgresql://gear_user:replace-with-password@gear-backend.lille.inria.fr:5432/gear_app
GEAR_TRACKING_ENABLED=true

# Production MLflow endpoint. The deployment writes the same value to /etc/gear.env.
MLFLOW_TRACKING_URI=http://gear-backend.lille.inria.fr:5000
GEAR_MLFLOW_ENABLED=true
MLFLOW_EXPERIMENT_NAME=gear-framework-production
GEAR_MLFLOW_LOG_OUTPUTS=true
GEAR_MLFLOW_MAX_LOG_CHARS=100000
MLFLOW_HTTP_REQUEST_TIMEOUT=5
MLFLOW_HTTP_REQUEST_MAX_RETRIES=1

# Wait briefly for the trace server to expose aggregate token and cost fields
# before the execution run is finalized. Increase only if your MLflow server is slow.
GEAR_MLFLOW_USAGE_WAIT_SECONDS=3
GEAR_MLFLOW_USAGE_POLL_INTERVAL_SECONDS=0.25

# Shared PIN known only by the experiment operators. It protects technical
# pause, resume, and technical-failure actions. Use at least 6 digits/characters.
GEAR_OPERATOR_PIN=123456789
